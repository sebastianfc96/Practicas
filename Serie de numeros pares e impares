// serie.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;

　
　
void main()
{int n,num,sign,ser,c1,c2;
 cout<<"Ingrese el numero por el cual comenzar";
 cin>>num;
 if(num%2==0)
 {
  
  sign=1;
   do
  {
   cout<<num<<endl;
   if(sign==1)
     num=num-3;
   else
	 num=num+1;
   sign=sign*-1;
   
  }while(num>0);
 }
 else
 {
  
  sign=1;
  do
 {cout<<num<<endl;
  if(sign==1)
	  num=num+1;
  else
	  num=num-3;
  sign=sign*-1;
  
 }while(num>0);
 
 
 }
 getch();
}
